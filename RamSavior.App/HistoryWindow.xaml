<ui:FluentWindow x:Class="RamSavior.App.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Icon="Assets/Icons/app.ico"         
        Title="History"
        Height="600" Width="1080"
        MinHeight="360" MinWidth="600"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="Recent cleaning history" Grid.Row="0"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,0,16,8">
            <ui:Button x:Name="ClearHistoryButton"
                       Content="Clear History"
                       Icon="{ui:SymbolIcon Symbol=Broom24}"
                       Appearance="Secondary"
                       ToolTip="Delete all cleaning history and start fresh — cannot be undone"
                       Click="ClearHistoryButton_Click"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="EntriesPanel" Margin="16,0,16,16" Orientation="Horizontal"/>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
